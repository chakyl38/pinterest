.col-md-6.col-md-offset-3
	.well
		%h1.text-center Edit Pin
		= image_tag @pin.image.url(:medium), class: "img-responsive center-block"

		= render 'form'

		= link_to 'Cancel', pin_path, class: 'btn btn-danger'